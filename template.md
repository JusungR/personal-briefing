# 브리핑 출력 템플릿

이 파일은 두 산출물(메일·아카이브 본문 / 텔레그램 요약)의 **캐노니컬 출력 형식**을
정의한다. 수집·검증·발송 절차와 콘텐츠 규칙(무엇을 넣을지·캡·신선도)은
`briefing-prompt.md`에 있다. 형식을 바꾸려면 이 파일만 고친다.

`{...}` 는 실제 값으로 치환할 자리표시자다. 매일 형식을 즉흥 재구성하지 말고
아래 레이아웃(구분선·이모지·번호 표기)을 **그대로** 따른다.

---

## A. 메일 / 아카이브 본문 (상세본)

Gmail 드래프트 본문이자 `archive/{KST_TODAY}.md`에 저장되는 형식. 아래 블록을
순서대로 이어 붙인다: 헤더 → 날씨 → 일정 → 뉴스 → 경제지표 → 원문 링크.

구분선은 `==================================================` (등호 50개)로 통일한다.

### A-1. 헤더

```
📋 Daily Briefing — {KST_TODAY}
==================================================
```

### A-2. 날씨 블록

```
☁️ 날씨 (서울, {M월 D일})

• 최저 {x}°C / 최고 {y}°C
• {강수 상태}, 강수 확률 {p}%
• 미세먼지: {등급} (초미세먼지 {등급})
• 💡 권고: {우산/겉옷/마스크 한 줄 권고}

==================================================
```

- 미세먼지 등급을 확인 못 하면 `미확인`으로 적는다(브리핑에 영향 없음).

### A-3. 일정 블록

```
📅 오늘 일정 ({KST_TODAY})

{HH:MM}  {제목}  ({위치 또는 회의 링크})
[종일] {제목}

==================================================
```

- 시간순으로 나열한다.
- 일정이 없으면 본문에 `오늘 등록된 일정 없음` 한 줄만 둔다.

### A-4. 뉴스 블록

지역(🇺🇸 미국 / 🇪🇺 유럽)으로 나누고, 각 지역 안에서 항목을 번호(`①②③…`)로
나열한다. 항목은 **헤드라인(한국어) → 1~2문장 요약 → 출처** 3줄 형식.

```
🌐 간밤 미국·유럽 주요 뉴스 ({NEWS_DATE})

────────────────────────
🇺🇸 미국
────────────────────────

① {한국어 헤드라인}
{1~2문장 한국어 요약.}
출처: {매체명}

② {한국어 헤드라인}
{1~2문장 한국어 요약.}
출처: {매체명}

────────────────────────
🇪🇺 유럽
────────────────────────

① {한국어 헤드라인}
{1~2문장 한국어 요약.}
출처: {매체명}

==================================================
```

- 같은 사건이 여러 매체에 나오면 최상위 1차 출처 한 곳만 적는다(병합 시 `다수 매체`).

### A-5. 경제지표 블록

```
📊 이번 주 경제지표 일정 ({KST_TODAY} ~ {KST_TODAY+6일})

{MM/DD} ({요일})
• {HH:MM} KST | {국기} {국가} | {지표명} | 예상: {예상치} (이전: {이전치})

{MM/DD} ({요일})
• {HH:MM} KST | {국기} {국가} | {지표명} | 예상: {예상치}

※ {보조 메모: 금주 FOMC/CPI 없음 등 — 있을 때만}

==================================================
```

- 날짜·시간은 KST 기준. 예상치·이전치가 없으면 생략.
- 발표 일정이 없는 날은 출력하지 않는다.

### A-6. 원문 링크 블록 (본문 최하단)

```
📎 원문 링크

[{한국어 헤드라인}] — {매체명}
{URL}

[{한국어 헤드라인}] — {매체명}
{URL}
```

- 본문에 인용된 기사 순서대로 나열한다.
- URL은 collect-news.py 출력의 url을 그대로 쓴다(Google News 리다이렉트도 그대로).
  WebSearch로 확인한 기사는 해당 WebSearch 결과 URL을 쓴다.

---

## B. 텔레그램 요약 (간략본)

`send_telegram.py`에 stdin으로 전달하는 Markdown 메시지. 뉴스는 헤드라인+출처만
한 줄로 요약한다(본문 설명 제외).

```
📋 *Daily Briefing — {KST_TODAY}*

☁️ *날씨 (서울)*
• 최저/최고 기온, 강수 확률
• 미세먼지 등급
• 한 줄 권고

📅 *오늘 일정*
• {HH:MM 제목}   (또는 "오늘 등록된 일정 없음")

📊 *이번 주 경제지표*
• {MM/DD 국기 지표명 (예상치)}

🌐 *간밤 주요 뉴스*
🇺🇸 미국
• {헤드라인} ({출처})
• {헤드라인} ({출처})
🇪🇺 유럽
• {헤드라인} ({출처})
• {헤드라인} ({출처})
```

- 총 길이가 4000자를 넘으면 뉴스 항목 수를 줄이되, **점수가 낮은 항목부터
  제거하고 관심분야(경제·시장·AI/기술)별 최소 1건은 유지**해 균형을 보존한다.
